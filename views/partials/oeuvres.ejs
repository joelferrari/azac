<%# Galerie d'accueil : la première œuvre de chaque artiste %>
<div class="works">
  <% artists.forEach((a) => { %>
    <%- include('work', { a, w: a.works[0], showArtist: true }) %>
  <% }) %>
</div>
